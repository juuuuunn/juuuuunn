# 実践例：プロンプトガイドを使った Todo アプリ開発

このドキュメントでは、提供したプロンプトガイドを使用して、実際に簡単な Todo アプリを段階的に開発するプロセスを示します。

**学べること：**
- どのプロンプトテンプレートを使うのか
- プロンプトをカスタマイズする方法
- プロンプト → 実装への流れ

---

## ステップ1: プロジェクト構造の計画

### 開発対象
シンプルな Todo アプリケーション
- Todo の追加、編集、削除
- 完了/未完了の切り替え
- ローカルストレージへの自動保存

### 技術スタック
- **フロントエンド**: React + TypeScript
- **スタイル**: Tailwind CSS
- **状態管理**: React Hooks（useContext）
- **ストレージ**: ローカルストレージ

### ファイル構成（案）
```
src/
├── components/
│   ├── TodoList.tsx         // メインコンポーネント
│   ├── TodoItem.tsx         // 個別の Todo
│   └── AddTodoForm.tsx      // 追加フォーム
├── contexts/
│   └── TodoContext.tsx      // 状態管理
├── hooks/
│   └── useTodo.ts          // カスタムフック
└── types/
    └── todo.ts             // 型定義
```

---

## ステップ2: 型定義とコンテキストの作成

### 使用するプロンプトテンプレート
**基本的なプロンプトテンプレート** → **新機能の実装**

### 送信するプロンプト

```
Todo アプリケーションの型定義とコンテキストを作成してください。

要件：
1. Todo 型の定義 (id, title, description, completed, createdAt)
2. TodoContext の作成（add, update, delete, toggle 関数）
3. useTodo カスタムフック

ファイル：
- src/types/todo.ts
- src/contexts/TodoContext.tsx
- src/hooks/useTodo.ts

詳細：
- ローカルストレージに自動保存
- TypeScript を使用
- Provider パターンで状態管理
```

### 期待される実装結果
- ✅ `src/types/todo.ts` に Todo インターフェース定義
- ✅ `src/contexts/TodoContext.tsx` に状態管理ロジック
- ✅ `src/hooks/useTodo.ts` に簡単に使えるカスタムフック

---

## ステップ3: UI コンポーネントの実装

### 使用するプロンプトテンプレート
**機能追加のプロンプト** → **UIコンポーネントの実装**

### 送信するプロンプト

```
TodoList コンポーネントを実装してください。

要件：
- Props: なし（useTodo フックで状態を取得）
- 表示内容: Todo リスト（完了/未完了の区別）
- インタラクション: 
  - 各 Todo の完了ボタン
  - 削除ボタン
  - 編集ボタン
- スタイル: Tailwind CSS

子コンポーネント：
- TodoItem（個別の Todo 表示）
- AddTodoForm（新規追加フォーム）

ファイル：
- src/components/TodoList.tsx
- src/components/TodoItem.tsx
- src/components/AddTodoForm.tsx
```

### ステップごとの実装

#### 3.1 AddTodoForm コンポーネント

```
AddTodoForm コンポーネントを実装してください。

要件：
- テキスト入力フィールド (title, description)
- 送信ボタン
- フォーム送信時に useTodo の add 関数を呼び出す
- 送信後、フォームをリセット
- バリデーション: title は必須

スタイル: Tailwind CSS
```

#### 3.2 TodoItem コンポーネント

```
TodoItem コンポーネントを実装してください。

Props：
- todo: Todo
- onToggle: (id: string) => void
- onDelete: (id: string) => void

表示内容：
- チェックボックス（完了状態の切り替え）
- タイトルと説明
- 作成日時
- 削除ボタン

完了時のスタイル：
- テキストに打ち消し線
- 色を薄くする

スタイル: Tailwind CSS
```

#### 3.3 TodoList コンポーネント

```
TodoList コンポーネントを実装してください。

要件：
- useTodo フックで Todo リストを取得
- AddTodoForm と TodoItem を組み合わせて表示
- 完了/未完了の分類表示（タブ形式）
- ローディング状態の処理

ファイル: src/components/TodoList.tsx
```

---

## ステップ4: 機能拡張 - フィルター機能

### 使用するプロンプトテンプレート
**機能追加のプロンプト** → **機能拡張**

### 送存するプロンプト

```
Todo リストにフィルター機能を追加してください。

新規追加：
- 「すべて」「進行中」「完了」のフィルタータブ
- フィルター状態を URL パラメータに保存
- フィルター結果の件数表示

対象ファイル：
- src/contexts/TodoContext.tsx（フィルター状態追加）
- src/components/TodoList.tsx（フィルタータブUI）
- src/hooks/useTodo.ts（フィルターロジック）

互換性：
- 既存の Todo データは変わらない
- 既存のテストはパスする
```

---

## ステップ5: パフォーマンス改善

### 使用するプロンプトテンプレート
**パフォーマンス最適化** → **パフォーマンス改善**

### 送信するプロンプト

```
Todo リスト表示のパフォーマンスを改善してください。

現在の問題：
- 1000個以上の Todo がある場合、レンダリングが遅い
- スクロール時にフレームが落ちる

目標：
- React.memo を使用した最適化
- 仮想スクロール（必要に応じて）
- 初期レンダリング時間を 500ms 以下に短縮

対象ファイル：
- src/components/TodoList.tsx
- src/components/TodoItem.tsx

測定方法：
- ブラウザの DevTools Performance タブで確認
```

---

## ステップ6: テストの追加

### 使用するプロンプトテンプレート
**テストとデバッグ** → **テストの追加**

### 送信するプロンプト

```
Todo アプリのテストを追加してください。

テスト対象：
- ファイル: src/hooks/useTodo.ts
- 関数: useTodo, 特に add, delete, toggle の動作

テストケース：
1. 新規 Todo 追加時、リストに追加される
2. Todo 削除時、リストから削除される
3. Todo 完了切り替え時、completed フラグが変更される
4. ローカルストレージに保存される
5. ページリロード後、データが復元される

テストフレームワーク: Vitest + React Testing Library
```

---

## ステップ7: バグ修正実例

仮に以下のようなバグが発生した場合：

### バグ報告

```
ページをリロードするとローカルストレージから todo が読み込まれないバグを修正してください。

再現手順：
1. Todo を複数追加
2. ページをリロード
3. Todo が表示されない（実際：表示されない）

期待動作：
ページをリロードしても、以前の Todo リストが表示される

対象：
ファイル: src/contexts/TodoContext.tsx（初期化処理周辺）
```

---

## ステップ8: 最終リファクタリング

### 使用するプロンプトテンプレート
**リファクタリング** → **コードの最適化**

### 送信するプロンプト

```
Todo アプリケーション全体をリファクタリングしてください。

改善目標：
- コンポーネントのロジックを分離（ビューとロジック）
- 重複するコードを削除
- コンポーネントのサイズを適正化（1つのコンポーネント < 150行）
- 型安全性を向上

制約：
- 機能は変わらない
- 既存のテストはパスする
- パフォーマンスは同等かそれ以上
```

---

## プロンプト作成の実践的なコツ

### 1. 段階的に実装する
```
❌ 一度に大きなプロンプト
「Todo アプリ全体を実装してください」

✅ 段階的に分割
「Step 1: 型定義とコンテキストを作成」
「Step 2: UI コンポーネントを実装」
「Step 3: フィルター機能を追加」
```

### 2. 既存のコードを参照
```
❌ 曖昧な参照
「このコンポーネントを修正」

✅ 具体的に参照
「src/components/TodoItem.tsx (行25-40) の完了ボタンをクリックすると...」
```

### 3. 要件を明確にする
```
❌ 曖昧な要件
「素敵な UI にしてください」

✅ 具体的な要件
「Tailwind CSS を使用し、
 - ボタンは青色（bg-blue-500）
 - 完了アイテムは灰色の打ち消し線
 - レスポンシブデザイン対応」
```

### 4. 制約条件を含める
```
✅ 制約を含める
「以下の条件で実装：
 - React 18.x
 - TypeScript を使用
 - 既存テストはパス
 - バンドルサイズ 200KB 以下」
```

---

## プロンプト→実装の流れまとめ

| フェーズ | プロンプト | 結果 |
|---------|----------|------|
| 計画 | 型定義・コンテキスト作成 | `types/`、`contexts/`、`hooks/` が完成 |
| UI 実装 | コンポーネント作成 | 各コンポーネントが実装される |
| 機能拡張 | フィルター追加 | 既存機能を保ちながら拡張 |
| 最適化 | パフォーマンス改善 | メモ化、スケーリング対応 |
| テスト | テスト追加 | 品質保証 |
| 修正 | バグ修正 | 問題解決 |
| 整理 | リファクタリング | 保守性向上 |

---

## チェックリスト：プロンプトを書く前に

- [ ] 何をしたいのか、明確か？
- [ ] ファイルパスを指定しているか？
- [ ] 要件や仕様が十分に詳しいか？
- [ ] 制約条件を含めているか？
- [ ] 期待される結果（ファイル、関数、UI）を示しているか？
- [ ] テスト方法を含めているか？

---

## 次のステップ

このパターンは、他のアプリケーション開発でも応用できます：

1. **新しいプロジェクト**：同じテンプレートで進める
2. **既存プロジェクトへの機能追加**：ステップ4以降を参考
3. **レガシーコード改善**：ステップ8のリファクタリング手法を参考
4. **バグ修正**：ステップ7のバグ報告プロンプト形式を参考

---

## 最終的なアプリケーション構成（完成イメージ）

```
src/
├── types/
│   └── todo.ts                 # Todo インターフェース
├── contexts/
│   └── TodoContext.tsx         # 状態管理・ロジック
├── hooks/
│   ├── useTodo.ts             # カスタムフック
│   └── useLocalStorage.ts      # (オプション)
├── components/
│   ├── TodoList.tsx           # メインコンポーネント
│   ├── TodoItem.tsx           # 個別アイテム（メモ化）
│   ├── AddTodoForm.tsx        # 追加フォーム
│   └── FilterTabs.tsx         # (拡張時)
├── __tests__/
│   └── useTodo.test.ts        # テスト
└── App.tsx                     # ルートコンポーネント
```

各ステップで、上記のファイルが段階的に完成していく流れが見えます。
