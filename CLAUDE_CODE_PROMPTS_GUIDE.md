# Claude Codeでのアプリ開発 - プロンプトガイド

Claude Codeを使用してアプリケーション開発を効率的に進めるためのプロンプト集とベストプラクティスです。

## 目次
1. [基本的なプロンプトテンプレート](#基本的なプロンプトテンプレート)
2. [機能追加のプロンプト](#機能追加のプロンプト)
3. [バグ修正のプロンプト](#バグ修正のプロンプト)
4. [リファクタリングのプロンプト](#リファクタリングのプロンプト)
5. [テストとデバッグ](#テストとデバッグ)
6. [パフォーマンス最適化](#パフォーマンス最適化)
7. [ベストプラクティス](#ベストプラクティス)

---

## 基本的なプロンプトテンプレート

### 1. 新機能の実装

```
[機能の説明]を実装してください。

実装内容：
- [具体的な要件1]
- [具体的な要件2]
- [具体的な要件3]

使用するファイル：
- [該当ファイル1]
- [該当ファイル2]
```

**使用例：**
```
ユーザー認証機能を実装してください。

実装内容：
- ログインフォーム (メール/パスワード)
- JWT トークンによるセッション管理
- ログアウト機能

使用するファイル：
- src/components/LoginForm.tsx
- src/services/auth.ts
- src/hooks/useAuth.ts
```

---

## 機能追加のプロンプト

### 2. APIエンドポイントの作成

```
[APIの説明] APIエンドポイントを作成してください。

仕様：
- メソッド: [GET/POST/PUT/DELETE]
- パス: /api/[path]
- リクエストボディ: [スキーマ]
- レスポンス: [スキーマ]
- エラーハンドリング: [詳細]
```

**使用例：**
```
ユーザープロフィール取得 APIエンドポイントを作成してください。

仕様：
- メソッド: GET
- パス: /api/users/:id
- パラメータ: user ID
- レスポンス: { id, name, email, avatar, createdAt }
- エラー: 404 User not found, 401 Unauthorized
```

### 3. UIコンポーネントの実装

```
[コンポーネント名] コンポーネントを実装してください。

要件：
- Props: [props一覧と型]
- 状態: [管理する状態]
- イベント: [ハンドラ一覧]
- スタイル: [スタイリング方式]
```

**使用例：**
```
ProductCard コンポーネントを実装してください。

要件：
- Props: { product: Product, onAddToCart: (id) => void }
- 表示内容: 商品名、価格、画像、説明
- インタラクション: ホバー効果、カートに追加ボタン
- スタイル: Tailwind CSS
```

---

## バグ修正のプロンプト

### 4. バグ修正の報告

```
[バグの症状]というバグが発生しています。

再現手順：
1. [手順1]
2. [手順2]
3. [手順3]

期待動作：
[正しく動作すべき内容]

実際の動作：
[現在の動作]

環境：
- ファイル: [該当ファイル]
- 行番号: [行番号（わかれば）]
```

**使用例：**
```
フォーム送信後にバリデーションエラーが消えないバグが発生しています。

再現手順：
1. フォームを開く
2. 不正な値を入力して送信
3. フォームをリセット

期待動作：
フォームをリセットしたときにエラーメッセージが消える

実際の動作：
エラーメッセージが表示されたままになる

環境：
- ファイル: src/components/Form.tsx
- 行番号: 42-50
```

### 5. パフォーマンス問題の修正

```
[パフォーマンスの問題]が発生しています。

現在の実装：
- [ファイルパス]
- [実装方式]

問題点：
- [具体的な問題]
- [パフォーマンスへの影響]

目標：
- [改善目標]
```

**使用例：**
```
大量のアイテムをレンダリングするときにページが遅い問題があります。

現在の実装：
- src/components/ItemList.tsx
- すべてのアイテムを一度にDOMにレンダリング

問題点：
- 1000個以上のアイテムがある場合、レンダリングに3秒以上かかる
- スクロール時にフレームが落ちる

目標：
- 仮想スクロール（Virtual Scroll）を導入
- 初期ロード時間を1秒以下に短縮
```

---

## リファクタリングのプロンプト

### 6. コードの最適化

```
[ファイル名]のコードをリファクタリングしてください。

現在の問題：
- [問題1]
- [問題2]

改善目標：
- [目標1]
- [目標2]

制約：
- 機能は変わらないようにする
- 既存のテストはすべてパスするようにする
```

**使用例：**
```
src/utils/dataTransform.tsのコードをリファクタリングしてください。

現在の問題：
- ネストが深い条件分岐が多い
- 関数が200行を超えている

改善目標：
- 関数を小さく分割する（単一責任の原則）
- 可読性を向上させる

制約：
- 機能は変わらないようにする
- パフォーマンスを損なわない
```

### 7. 重複コードの整理

```
[複数のファイル]に重複するコードがあります。

対象ファイル：
- [ファイル1]
- [ファイル2]
- [ファイル3]

共通化する内容：
[重複しているコードの説明]

作成方法：
- [ユーティリティ関数 / 共通コンポーネント / カスタムフック など]
```

**使用例：**
```
複数のコンポーネントで日付フォーマット処理が重複しています。

対象ファイル：
- src/components/EventCard.tsx
- src/components/TimelineItem.tsx
- src/components/BlogPost.tsx

共通化する内容：
日付を "YYYY年MM月DD日 HH:mm" 形式にフォーマットする処理

作成方法：
src/utils/dateFormatter.ts にユーティリティ関数を作成
```

---

## テストとデバッグ

### 8. テストの追加

```
[機能名]のテストを追加してください。

テスト対象：
- ファイル: [ファイルパス]
- 関数/コンポーネント: [名前]

テストケース：
1. [ケース1]
2. [ケース2]
3. [ケース3]

テストフレームワーク: [Jest / Vitest / Playwright など]
```

**使用例：**
```
calculateTotal 関数のテストを追加してください。

テスト対象：
- ファイル: src/utils/calculate.ts
- 関数: calculateTotal

テストケース：
1. 空の配列を渡したときに0を返す
2. 複数の数値を渡したときに合計を返す
3. 小数を含む数値を正確に計算する

テストフレームワーク: Vitest
```

### 9. デバッグの支援

```
[機能]がデバッグが必要です。

状況：
- [何を試しているのか]
- [期待される結果]
- [実際の結果]

調査対象：
- ファイル: [ファイルパス]
- 関数/メソッド: [名前]

提供できる情報：
- [コンソール出力]
- [エラーメッセージ]
- [その他の情報]
```

---

## パフォーマンス最適化

### 10. バンドルサイズの削減

```
バンドルサイズを削減してください。

現在のサイズ：
- [メインバンドル]: [サイズ]
- [その他]: [サイズ]

目標：
- [削減目標]

対象：
- [最適化対象のファイル/ライブラリ]
```

### 11. 読み込み時間の短縮

```
ページの読み込み時間を短縮してください。

現在の状況：
- First Contentful Paint: [時間]
- Largest Contentful Paint: [時間]
- Cumulative Layout Shift: [スコア]

目標：
- [改善目標]

最適化方法：
- [提案がある場合]
```

---

## ベストプラクティス

### プロンプト作成の原則

#### 1. **具体的に書く**
```
❌ 悪い例：
「ログイン機能を作ってください」

✅ 良い例：
「メールアドレスとパスワードでログイン後、JWT トークンをローカルストレージに保存し、
API リクエストのヘッダーに含めるログイン機能を実装してください。
src/components/LoginForm.tsx と src/services/auth.ts を使用してください。」
```

#### 2. **ファイルパスと行番号を指定**
```
❌ 悪い例：
「この関数を修正してください」

✅ 良い例：
「src/utils/api.ts の fetchData 関数（32-45行）をリファクタリングしてください。
エラーハンドリングを改善し、タイムアウト機能を追加してください。」
```

#### 3. **制約条件を明確にする**
```
❌ 悪い例：
「パフォーマンスを改善してください」

✅ 良い例：
「パフォーマンスを改善してください。
制約：
- React の 18.x を使用
- Redux の状態管理は変更しない
- 既存のテストはすべてパスさせる
目標：
- 初期読み込み時間を3秒以下に削減」
```

#### 4. **背景情報を提供**
```
❌ 悪い例：
「新しいフィルター機能を追加してください」

✅ 良い例：
「ユーザーからのリクエストで、商品一覧にカテゴリーフィルター機能を追加してください。
背景：
- ユーザーは複数のカテゴリーを同時に選択したい
- フィルター結果はURLパラメータに保存される
- 既存のソート機能との併用が必要

実装対象：
- src/components/ProductFilter.tsx（新規作成）
- src/pages/Products.tsx（既存）」
```

### よく使うプロンプトパターン

#### パターン1: 新機能実装
```
[機能説明]を実装してください。

仕様：
- [詳細な要件1]
- [詳細な要件2]

対象ファイル：
- [ファイル1]
- [ファイル2]

完了条件：
- [テスト/チェックリスト]
```

#### パターン2: バグ修正
```
[バグの説明]を修正してください。

再現手順：
1. [ステップ1]
2. [ステップ2]

期待動作：
[正しく動作すべき動作]

対象ファイル：
[該当ファイル]

調査ポイント：
- [疑わしい箇所]
```

#### パターン3: リファクタリング
```
[ファイル]をリファクタリングしてください。

改善目標：
- [目標1]
- [目標2]

制約：
- 動作は変わらない
- 既存テストはパスする

提案あれば：
[改善案がある場合]
```

#### パターン4: 調査と分析
```
[問題/質問]について調査してください。

調査範囲：
- [ファイル/ディレクトリ]

質問事項：
- [質問1]
- [質問2]

リポート内容：
- [報告形式の指定]
```

---

## 実践的なシナリオ別プロンプト

### シナリオ1: 新しいページを追加する
```
新しいユーザーダッシュボードページを作成してください。

要件：
1. ルート: /dashboard
2. 表示内容:
   - ユーザーの基本情報（名前、メール、プロフィール画像）
   - 最近のアクティビティ（テーブル形式、10件表示）
   - 統計情報（登録日数、総投稿数、フォロワー数）
3. 機能:
   - データは API から取得 (GET /api/users/:id/dashboard)
   - ローディング状態を表示
   - エラーハンドリング（404, 500など）

ファイル構成:
- src/pages/Dashboard.tsx （ページコンポーネント）
- src/components/UserInfo.tsx （ユーザー情報カード）
- src/components/ActivityTable.tsx （アクティビティテーブル）
- src/components/Stats.tsx （統計情報）
- src/hooks/useDashboard.ts （データフェッチング）

スタイル: Tailwind CSS
```

### シナリオ2: 既存機能を拡張する
```
商品フィルター機能を拡張してください。

現在の機能:
- カテゴリーフィルター
- 価格範囲フィルター

新規追加:
- ブランドマルチセレクト
- 在庫ステータス (在庫あり/なし)
- 評価スコア (3つ星以上など)
- フィルターの保存機能（ユーザー設定として）

対象ファイル:
- src/components/ProductFilter.tsx
- src/hooks/useProductFilter.ts
- src/store/filterSlice.ts

テスト:
- フィルター適用時の結果件数の確認
- 複数フィルターの同時使用
- フィルター保存・読み込み
```

### シナリオ3: パフォーマンス最適化
```
商品リスト（1000件以上）のパフォーマンスを最適化してください。

現在の問題:
- 初期読み込みに5秒以上かかる
- スクロール時にフレームが落ちる（FPS < 30）

対策:
- 仮想スクロール（react-window または同等）の実装
- 画像の遅延読み込み
- メモ化の適切な使用
- バンドルサイズの確認

対象ファイル:
- src/pages/Products.tsx
- src/components/ProductCard.tsx
- src/components/ProductList.tsx

測定:
- Lighthouse スコア
- Core Web Vitals
- 初期読み込み時間
```

---

## チェックリスト

プロンプトを送信する前に確認:

- [ ] 目的が明確か
- [ ] ファイルパスを指定しているか
- [ ] 実装対象の範囲が明確か
- [ ] 制約条件を含めているか
- [ ] 期待される動作を説明しているか
- [ ] 必要に応じて背景情報を含めているか
- [ ] テスト方法やチェック項目を含めているか
- [ ] 技術スタック（言語、ライブラリなど）を指定しているか

---

## まとめ

効果的なプロンプトの要素:
1. **明確性** - 何をしたいのかをはっきり伝える
2. **具体性** - ファイルパスや行番号などの詳細を含める
3. **背景** - なぜそれが必要なのかを説明する
4. **制約** - 何をしてはいけないかを明示する
5. **測定** - 完了をどう確認するかを示す

これらの要素を含めることで、Claude Code がより正確で効率的に対応できます。
